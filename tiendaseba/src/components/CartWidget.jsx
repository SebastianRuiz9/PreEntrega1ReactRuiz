import React from 'react';

const CartWidget = () => {
  const itemCount = 5; 

  return (
    <div className>
      <i className></i>
      <span className>{itemCount}</span>
    </div>
  );
};

export default CartWidget;
